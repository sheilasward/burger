<li>
	{{burger_name}}
	{{#if eat}}
		<button class="change-devoured" data-id="{{id}}" data-neweat="{{eat}}">
		Devour it!
		</button>
	{{/if}}
	<button class="delete-burger" data-id="{{this.id}}">Delete Burger</button>  
</li>
